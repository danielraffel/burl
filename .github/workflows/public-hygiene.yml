name: Public hygiene

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  public-hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python3 tools/scripts/public_hygiene_check.py
